{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://patrick-werner.github.io/fhir-biomarker-examples/schema/metadata.schema.json",
  "title": "Submission metadata",
  "description": "Metadata of one submission. Only 'title' and 'origin' are required; everything else is defaulted from validation.config.yaml, derived from the FHIR resources or taken from contributor.yaml.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "origin"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 3,
      "maxLength": 120,
      "description": "Short human readable title of the submission."
    },
    "origin": {
      "type": "string",
      "enum": ["real-world", "derived-from-real", "synthetic"],
      "description": "Where the example comes from. 'real-world' and 'derived-from-real' earn rating level 1."
    },
    "description": {
      "type": "string",
      "minLength": 3,
      "maxLength": 2000,
      "description": "One or two sentences. Longer prose belongs in README.md."
    },
    "fhirVersion": {
      "type": "string",
      "enum": ["4.0.1", "4.3.0", "5.0.0"],
      "description": "Defaults to validation.config.yaml defaultFhirVersion."
    },
    "igs": {
      "type": "array",
      "maxItems": 20,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9][a-z0-9.\\-]*#(?!current$|dev$|latest$)[A-Za-z0-9][A-Za-z0-9.\\-+]*$",
        "description": "Package id and pinned version, e.g. hl7.fhir.eu.laboratory#2.0.0. Floating versions (#current, #dev, #latest) are rejected."
      },
      "description": "Additional implementation guides, loaded on top of the defaults for this FHIR version."
    },
    "category": {
      "type": "string",
      "enum": ["lab", "genomic", "imaging", "other"],
      "description": "Coarse grouping for the site. Defaults to 'other'."
    },
    "biomarkers": {
      "type": "array",
      "maxItems": 50,
      "items": {
        "oneOf": [
          { "type": "string", "minLength": 1, "maxLength": 100 },
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["name"],
            "properties": {
              "name": { "type": "string", "minLength": 1, "maxLength": 100 },
              "system": { "type": "string", "minLength": 1, "maxLength": 300 },
              "code": { "type": "string", "minLength": 1, "maxLength": 100 }
            }
          }
        ]
      },
      "description": "Optional. When omitted the codes are derived from Observation.code.coding."
    },
    "custodian": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 2, "maxLength": 100 },
        "github": { "type": "string", "pattern": "^[A-Za-z0-9-]{1,39}$" },
        "contact": { "type": "string", "minLength": 3, "maxLength": 200 }
      },
      "description": "Who answers questions about this submission. Defaults to contributor.yaml."
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "vendor": { "type": "string", "minLength": 1, "maxLength": 200 },
        "system": { "type": "string", "minLength": 1, "maxLength": 200 },
        "project": { "type": "string", "minLength": 1, "maxLength": 200 },
        "url": { "type": "string", "pattern": "^https?://", "maxLength": 300 }
      },
      "description": "Where the example was produced."
    },
    "knownIssues": {
      "type": "array",
      "maxItems": 50,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["justification"],
        "anyOf": [
          { "required": ["messageId"] },
          { "required": ["file"] },
          { "required": ["location"] },
          { "required": ["details"] }
        ],
        "properties": {
          "messageId": {
            "type": "string",
            "minLength": 2,
            "maxLength": 200,
            "description": "Validator message id, matched case-insensitively."
          },
          "file": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "description": "File name inside this submission, matched exactly."
          },
          "location": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300,
            "description": "FHIRPath expression or 'Line n, Column m'. Supports * wildcards."
          },
          "details": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500,
            "description": "Message text. Supports * wildcards."
          },
          "justification": {
            "type": "string",
            "minLength": 10,
            "maxLength": 1000,
            "description": "Why this issue is accepted. Required."
          }
        }
      },
      "description": "Issues that are known and accepted. All given matchers must match. Suppressed issues do not affect the status but are counted and shown."
    },
    "review": {
      "type": "object",
      "additionalProperties": false,
      "description": "Maintainers only.",
      "properties": {
        "smeReviewed": { "type": "boolean" },
        "consensus": { "type": "boolean" },
        "reviewer": { "type": "string", "minLength": 2, "maxLength": 200 },
        "date": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
        "notes": { "type": "string", "minLength": 3, "maxLength": 2000 },
        "normalizedVersion": {
          "type": "string",
          "minLength": 3,
          "maxLength": 200,
          "description": "Path of an improved variant of this submission."
        }
      }
    }
  }
}
